const string = require('./string.validation');
const number = require('./number.validation');
const date = require('./date.validation');
const file = require('./file.validation');

module.exports = {
  string,
  number,
  date,
  file
}; 